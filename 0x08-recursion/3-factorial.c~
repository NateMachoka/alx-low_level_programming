#include <stdio.h>
#include "main.h"

/**
 * factorial - calculates the factorial of any given number
 * @n - the given number
 *
 * Return: integer value
 */

int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	else if (n == 0)
	{
		return (1);
	}
	else
		return (n * factorial(n - 1));
}
